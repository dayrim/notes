const config = {
  appId: "net.connection.tribe",
  appName: "notes",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
  electron: {
    deepLinkingEnabled: true,
  },
};

export default config;
